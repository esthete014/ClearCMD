#pragma once
#include <iostream>
#include <vector>
#include "block.h"

class Visual {
public:
	Visual(std::vector<std::vector<Block<TYPE>>> layout) {
		for (int i = 0; i < layout.size(); i++) {
			for (int j = 0; j < layout[i].size(); j++) {
				layout[i][j].type
			}
		}
	}
private:


};